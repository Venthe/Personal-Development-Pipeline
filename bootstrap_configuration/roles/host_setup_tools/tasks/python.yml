# TODO: Run in .venv
- name: Ensure pip is installed
  become: true
  ansible.builtin.package:
    name: python3-pip
    state: present

- name: Ensure Python dependencies for Kubernetes module are installed
  become: true
  ansible.builtin.package:
    name: python3-kubernetes
    state: present
