#!/bin/bash

# save incoming YAML to file
cat <&0 > all.yaml

# modify the YAML with kustomize
kubectl kustomize ./ > result.yaml && true

# diff all.yaml result.yaml

# function cleanup()
# {
#     echo "Cleanup"
#     # rm all.yaml
# }

# trap cleanup EXIT SIGINT
