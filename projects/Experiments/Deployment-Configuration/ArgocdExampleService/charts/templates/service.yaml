apiVersion: v1
kind: Service
metadata:
  name: {{ include "ArgocdExampleService.fullname" . }}
  labels:
    {{- include "ArgocdExampleService.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ArgocdExampleService.selectorLabels" . | nindent 4 }}
